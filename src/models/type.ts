export type IP_INFO = {
  ip: string;
  country_name?: string;
  region_name?: string;
  city?: string;
  latitude: number;
  longitude: number;
};

export type SUNSET_SUNRISE_TIME = { sunrise: string; sunset: string };
